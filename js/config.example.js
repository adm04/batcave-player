/**
 * Batcave Player - Client Runtime Environment Configuration Template
 * 
 * Instructions:
 * 1. Copy this file to `js/config.js` in the same directory.
 * 2. Fill in your real credentials in `js/config.js`.
 * 3. Note: `js/config.js` is included in `.gitignore` to prevent committing secrets to GitHub.
 */

window.ENV = {
  SUPABASE_URL: 'YOUR_SUPABASE_URL',
  SUPABASE_KEY: 'YOUR_SUPABASE_PUBLISHABLE_KEY'
};
